# ── rl-trading-live requirements.txt ────────────────────────────────────────
# Pinned for reproducibility. Tested on Python 3.10+ (Colab A100).
# Torch is installed by Colab's CUDA image; pinned here for local/dev parity.
torch==2.7.1
# numpy<2.0 required by faiss-cpu; numpy>=1.26 required by pandas 2.x
numpy>=1.26,<2.0
pandas>=2.0,<3.0
PyYAML==6.0.2
python-dotenv==1.0.1
requests==2.32.3

# Dashboard
streamlit==1.39.0
streamlit-autorefresh==1.0.1
plotly==5.24.1

# Jordan repo indexing (FAISS) — optional at runtime; guarded imports
faiss-cpu==1.8.0.post1

# Broker (Windows live only; import-guarded so Colab/Linux still works)
# MetaTrader5==5.0.5120   # uncomment on Windows; not installable on Linux/Colab

# Indicators — REQUIRED single source of truth (DESIGN_DECISIONS.md #3)
# On Colab: apt-get install -y ta-lib first. On Windows: install the TA-Lib wheel.
TA-Lib==0.4.32

# Test + lint
pytest==8.3.3
pytest-cov==5.0.0
black==24.10.0
ruff==0.7.1
